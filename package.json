{
  "name": "multisig-wallet-dao",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "M-of-N multisig wallet with DAO treasury and time-lock controls.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "coverage": "hardhat coverage",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "chai": "^4.4.1",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.5"
  }
}
